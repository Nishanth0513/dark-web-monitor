# Dark Web Breach Monitor (Streamlit)

Run:

```bash
python -m pip install -r requirements.txt
streamlit run streamlit_app.py
```

Features:
- Individual mode (personal email monitoring + risk score)
- Enterprise mode (org + many employee emails + aggregated org risk)
- Password monitor (HIBP Pwned Passwords k-anonymity check; password is never stored)